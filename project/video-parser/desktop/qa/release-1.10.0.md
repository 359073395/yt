# 1.10.0 验收记录

日期：2026-09-24 至 2026-09-25。用户明确批准按当前已实现功能范围正式发布，不再等待旧规划全部完成。
未实现/未验证项目在 `RELEASE-1.10.0.md` 公开列出，不将它们改成“通过”。

## 1. 界面与交互

依据用户本轮提供的任务页和直播页截图：字号层级过多，输入区/封面太高，任务区嵌套滚动，侧栏底部难以看到。
保留现有导航与橙色主色，调整布局，不引入新 UI 库或额外运行时。

1. 视频任务：主内容区统一滚动；封面与输入区缩小。根据用户追加要求，全界面只保留正文 16、辅助 14、标题/品牌 20 三档字号，任务页和模型窗口均检查了浏览器计算样式。1280×800 下任务卡片按钮与飞书自动下载均完整可见，低矮窗口通过主内容滚动到达。
2. 小窗口：1280×720、1000×640、800×560 均无文档横向溢出；导航底部不超过页头，主内容底部等于视口，侧栏底部距视口 16 px。导航需要时分两行，不与内容重叠。
3. 直播：模拟创建、停止、下滑到记录卡片；1000×640 卡片底部 624 px，打开文件夹按钮完整可见。UI 模拟不作为真实录像证据。
4. 模型：1000×640 下滚动到底部，“保存翻译方式”按钮底部约 591 px，未裁切。模型位置、上游模型入口仍保留。

截图/尺寸原始证据位于本机 `F:\820\yinglian-personal-qa\release-1.10.0\ui`。
以上是布局、滚动和基本交互验收，不声称完整 WCAG 或全部辅助技术合规。

## 2. 飞书分类

旧代码仅接受带冒号的分类标签，并对重复视频只去重、不更新分类。
用户的实际消息末尾为 `... :4pm 分类 测试`。本轮加入完整分享文案、NBSP、Markdown 链接、中文标题的回归用例，确认入库只有一条且分类为“测试”。

覆盖有/无冒号、等号、制表符、嵌套文件夹、分类后备注、重复链接更新分类、重复裸链接不重置分类、仅补备注保留分类、越界/Windows 设备名称拒绝。平台原有 hashtag 不会随意变成文件夹。
既有真实双员工双电脑验收没有在本轮完成；单元测试的身份隔离不冒充该实测。

## 3. 自动化回归与构建

- 前端 64 项通过（含 4 个全界面三档字号约束测试，以及删除、活动任务保护、订阅旧轮询去重等新增测试）。
- Rust 原生 76 项通过；1 项会访问真实飞书注册接口的测试显式忽略。新增飞书删除后重启/历史回放不恢复、保留文件、活动/其他配对记录拒绝删除、写入失败原记录保留测试。
- TikTok 直播插件 12 项通过（独立 QA Python 依赖不随包分发）。
- 生产前端构建通过；主入口 JS 约 289 kB，未增加前端依赖；开发模拟桥接不进入生产包。
- `npm audit --omit=dev`：0 项已报告漏洞。
- React 检查保留现有轮询清理和异步下载，不在渲染过程中发起网络请求；直播详情采用原生 details，不新增庞大组件依赖。

## 4. 真实下载回归

同源 1.10.0 原生 `account-smoke` 使用应用自己的会话缓存、代理和正式下载函数，不启动飞书收件线程，不修改原任务。

- 用户抖音链接 `https://v.douyin.com/B6-Fzz29S_Y/`：预览与下载通过，约 23 秒；视频 401,292 字节、8.080 秒、HEVC 720×1280 + AAC，并有封面。完整 FFmpeg 解码退出 0。
- 上游 TikTok 挂商品样本 `https://www.tiktok.com/@fujiiian/video/7460373937537551634`：预览成功，但下载阶段官方嵌入页返回 HTTP 503。本轮记为失败，不计入通过，不保证该类视频全部支持。
- 本轮未重测 YouTube/哔哩哔哩全部下载与本地/API 翻译质量。旧测试记录不能替代本版本全部平台通过。

## 5. 直播

同日已实测原用户失败直播间的 FLV 兼容补丁，详见 `tiktok-shop-20260924.md`。两次文件分别为 12.040 秒与 6.316 秒，均有 H.264 640×1280 + AAC 并通过完整解码。
本次安装包纳入同一 SHA-256 的 3,961 字节插件。短录制成功不代表长时稳定性、断线续录或其他直播间全覆盖。

## 6. 安装与发布

- 本地最终生产安装包 68,373,632 字节，SHA-256 `6D86E1C7DA52BC2D035AF2172F957111C04C2A28ECF0A003B5C03DD065002F24`；NSIS 静默覆盖原安装目录，退出 0。
- 覆盖前确认飞书任务为 1 完成 / 2 历史失败，没有活动下载或录制。备份 31 个设置/登录相关文件，并保留旧程序；安装后首次启动前逐项 SHA-256 完全一致。备份含私密信息，仅留本机，不提交仓库。
- 安装版 ProductVersion、窗口标题均为 1.10.0；更新入口不再显示试用版。飞书仍为已配对、已连接，保存目录、Medium 模型、本地翻译均保留。两条历史 Cookie 错误记录未删除，也未冒充新版本成功。
- 本机工作区为 1536×816 逻辑像素；实际原生窗口起点 (159,16)、外框 1282×783，底部 799，小于任务栏上缘 816。没有更改 Windows 缩放或任务栏设置。
- 插件 SHA-256 与直播实测一致；独立旧版稳定程序 SHA-256 未改变。
- GitHub 流程将在草稿 Release 中生成签名包，使用随应用发布的公钥独立验证包与可信注释签名，并做篡改负对照；签名包需再次检查后才公开发布。

## 7. 删除记录增补

2026-09-25 用户追加下载记录不能删除。本轮为手动和飞书卡片添加逐条删除及确认对话框，保留手动批量入口。只删除记录，不调用输出目录删除接口；写入成功后才显示成功。

- 模拟界面实际操作：处理中卡片按钮禁用；已完成卡片可打开确认框；取消后记录保留，确认后卡片移除。没有用用户真实记录进行删除测试。
- Rust 测试在独立临时目录创建样本视频，删除记录并重新读盘，原视频字节保持一致；飞书消息 ID 去重表仍保留，重放旧消息不再入队。
- 主动发送新的飞书消息仍可重新下载同一链接，区别于旧消息回放。
- 订阅删除保存已处理 receipt ID，重启及未确认轮询不会复活任务，新的订阅作品不受影响。
- 包含本增补的安装包 68,369,826 字节，SHA-256 `2AC65C9D415EE9563654F668598B504BEB354CFA6FC0B750CCAF41BAB20C9A68`；2026-09-25 再次覆盖原目录安装，退出 0，31 个配置/登录文件安装前后 SHA-256 完全一致。窗口标题/产品版本 1.10.0，实际卡片底部已出现删除按钮，飞书仍已配对、已连接，Medium 和本地翻译选择保留。
- 原生窗口外框仍为 (159,16) 1282×783，未侵入任务栏；UI 取消/确认和批量测试使用开发模拟数据，原生后端删除文件保留与持久性使用隔离临时目录；未为测试删除用户既有记录。
- 用户新发的真实飞书视频已自动归入“测试”，视频 106,073,147 字节、封面 28,024 字节。本次 16 分钟视频的语音识别经用户明确允许中断，保留为部分完成，不计为文案成功。
- 最终 CI 签名包仍须独立校验后再公开发布。

## 8. 最终签名包与公开发布

2026-09-25：GitHub Actions `36026757538` 全部通过，应用源码对应 `8e21a464746df11df8416c37831dcbbcf53c5780`。

- 公开安装包 `Paoliang-YingLian_1.10.0_x64-setup.exe`：75,366,005 字节；SHA-256 `502d0164d1247a67e02aedb8670e503dc517474f6ba0f16f07ce6bee22b0e33b`。
- CI 与本机独立检查均通过应用内置公钥验证；修改摘要后验证失败；更新清单两个 Windows 平台的签名与安装包 `.sig` 一致。
- 该签名包已再次覆盖原 D 盘安装目录，NSIS 退出 0；31 个配置/登录文件安装前后逐项 SHA-256 相同。主程序版本 1.10.0、进程正常响应；独立旧稳定版 exe 校验和未改变。
- 安装后的官方运行时：yt-dlp 2026.08.19、whisper.cpp b4938、FFmpeg N-126826-gc0e8b139fd。使用该 FFmpeg 对本轮下载的 8 秒抖音视频完整解码，退出 0；直播插件校验和保持与本轮实测一致。
- Release 已公开且非预发布，设为最新；安装包公开地址返回 HTTP 200、application/octet-stream。更新清单使用公开二进制下载地址，不依赖 GitHub API asset 元数据响应；旧版渠道仍为 1.1.8。
- 本机配置备份、账号信息及真实下载文件不随 Release 上传。公开包附有 SHA256SUMS.txt。
